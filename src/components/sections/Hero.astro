---
import Button from "../ui/Button.astro";
import GlassCardStack from "../react/GlassCardStack";
import AnimatedBackground from "../react/AnimatedBackground"; // Import the new background
---

<section class="relative min-h-[92vh] flex items-center pt-24 pb-12 overflow-hidden bg-white">
  
  {/* Load the animated background (client:load ensures it animates immediately) */}
  <AnimatedBackground client:load />

  <div class="container mx-auto px-6 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      
      {/* --- RIGHT COLUMN: Content --- */}
      <div class="lg:col-span-7 text-right space-y-8 order-2 lg:order-1">
        
        {/* Animated Badge */}
        <div class="opacity-0 animate-fade-in-up" style="animation-delay: 0.1s;">
            <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-white/50 border border-primary/10 shadow-sm backdrop-blur-sm transition-transform hover:scale-105 cursor-default">
            <span class="relative flex h-2.5 w-2.5">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-primary"></span>
                </span>
            <span class="text-primary text-xs font-bold tracking-wide">وكالة إراتيك الرقمية</span>
            </div>
        </div>

        {/* Headline */}
        <h1 class="text-5xl md:text-7xl lg:text-8xl/none font-black text-slate-900 tracking-tight opacity-0 animate-fade-in-up" style="animation-delay: 0.2s;">
          نحول أفكارك إلى <br />
          <span class="text-transparent bg-clip-text bg-gradient-to-l from-primary via-blue-600 to-purple-600 animate-gradient-x">
             واقع ملموس
          </span>
        </h1>

        {/* Description */}
        <p class="text-xl md:text-2xl text-slate-500 max-w-2xl leading-relaxed font-normal opacity-0 animate-fade-in-up" style="animation-delay: 0.3s;">
          نحن لا نبني مواقع إلكترونية فحسب، بل نصنع <span class="text-slate-800 font-semibold decoration-primary/30 underline decoration-2 underline-offset-4">تجارب رقمية فاخرة</span> تعزز مكانة علامتك التجارية.
        </p>

        {/* Buttons */}
        <div class="flex flex-col sm:flex-row gap-4 pt-4 opacity-0 animate-fade-in-up" style="animation-delay: 0.4s;">
          <Button size="lg" class="h-14 px-10 rounded-2xl text-lg shadow-xl shadow-primary/20 hover:shadow-primary/40 hover:-translate-y-1 transition-all">
            ابدأ مشروعك
          </Button>
          
          <a href="/services" class="group inline-flex items-center justify-center h-14 px-8 rounded-2xl text-lg font-semibold text-slate-600 bg-white border border-slate-200 shadow-sm hover:border-primary/30 hover:text-primary transition-all duration-300">
             <span>تصفح أعمالنا</span>
             <svg xmlns="http://www.w3.org/2000/svg" class="mr-2 w-5 h-5 transform group-hover:-translate-x-1 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
          </a>
        </div>

        {/* Trust Indicators */}
        <div class="pt-10 flex items-center gap-6 opacity-0 animate-fade-in-up" style="animation-delay: 0.5s;">
            <div class="flex -space-x-3 space-x-reverse">
                {[1,2,3,4].map(() => (
                    <div class="w-10 h-10 rounded-full border-2 border-white bg-slate-200"></div>
                ))}
            </div>
            <div class="text-sm font-medium text-slate-600">
                انضم لأكثر من <span class="font-bold text-slate-900">+150</span> عميل سعيد
            </div>
        </div>
      </div>

      {/* --- LEFT COLUMN: 3D Visual --- */}
      <div class="lg:col-span-5 order-1 lg:order-2 relative h-[500px] w-full perspective-1000 flex items-center justify-center opacity-0 animate-fade-in-up" style="animation-delay: 0.4s;">
         <div class="relative z-10 scale-110 lg:scale-125 hover:scale-[1.3] transition-transform duration-700 ease-out">
             <GlassCardStack client:visible />
         </div>
      </div>

    </div>
  </div>
</section>